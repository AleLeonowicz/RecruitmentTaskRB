import React from 'react';
import classes from './Category.module.scss';

const Category = () => {
  return (
    <div className={classes.category_container}>
      <div className={classes.category_innerContainer}></div>
    </div>
  );
};

export default Category;
